import { SliderItem } from "app/models/sliderItem";

export class Slider {
    Interval: number;
    Height: number;
    SliderID: string;
    Disabled: boolean;
    ShowPrevNext: boolean;
    ShowSlideIndicators: boolean;
    CreatedTime: Date;
}